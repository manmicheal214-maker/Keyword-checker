'use strict';
const fs=require('fs');const path=require('path');
const ROOT=path.resolve(__dirname,'..');
const F={k:path.join(ROOT,'config/keywords.json'),s:path.join(ROOT,'config/settings.json'),r:path.join(ROOT,'data/rankings.json'),h:path.join(ROOT,'data/history.json')};
const KEY=process.env.ZENROWS_API_KEY;
const D={country:'India',country_code:'',language:'en',search_engine:'google',google_tld:'',max_keywords_per_domain:50,max_domains:5,max_position:100,request_delay_ms:2000,request_timeout_ms:60000,max_retries:2,history_days:365};
const read=(f,d)=>{if(!fs.existsSync(f))return d;const s=fs.readFileSync(f,'utf8');return s.trim()?JSON.parse(s):d};
const write=(f,d)=>{const t=f+'.tmp';fs.writeFileSync(t,JSON.stringify(d,null,2)+'\n');fs.renameSync(t,f)};
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function dom(v){try{return new URL(/^https?:\/\//i.test(v)?v:'https://'+v).hostname.toLowerCase().replace(/^www\./,'').replace(/\.$/,'')}catch{return null}}
function match(link,target){try{const h=new URL(link).hostname.toLowerCase().replace(/^www\./,'').replace(/\.$/,'');return h===target||h.endsWith('.'+target)}catch{return false}}
function firstUrl(k){const u=new URL('https://serp.api.zenrows.com/v1/targets/google/search/'+encodeURIComponent(k));u.searchParams.set('apikey',KEY);return u}
function pageUrl(next){const u=new URL('https://serp.api.zenrows.com/v1/targets/google/search');u.searchParams.set('url',next);u.searchParams.set('apikey',KEY);return u}
async function get(u,s){let last;for(let a=0;a<=Number(s.max_retries);a++){const c=new AbortController(),tm=setTimeout(()=>c.abort(),Number(s.request_timeout_ms));try{const r=await fetch(u,{headers:{Accept:'application/json'},signal:c.signal}),txt=await r.text();let d=null;try{d=txt?JSON.parse(txt):null}catch{}if(!r.ok)throw new Error('ZenRows HTTP '+r.status+(d?.detail?': '+d.detail:''));if(!d||typeof d!=='object')throw new Error('ZenRows returned invalid JSON.');return d}catch(e){last=e;if(a<Number(s.max_retries)){const ms=Math.min(10000,1000*2**a);console.warn('Request failed. Retrying in '+ms+'ms...');await sleep(ms)}}finally{clearTimeout(tm)}}throw last}
async function serp(k,s){const wanted=Number(s.max_position),need=Math.max(1,Math.ceil(wanted/10));let u=firstUrl(k),all=[],pages=0;while(u&&pages<need){const d=await get(u,s),rows=Array.isArray(d.organic_results)?d.organic_results:[];all.push(...rows);pages++;if(all.length>=wanted||!rows.length)break;const n=d.pagination?.next_page;if(!n)break;u=pageUrl(n);if(Number(s.request_delay_ms)>0)await sleep(Number(s.request_delay_ms))}return{rows:all.slice(0,wanted),pages}}
function find(rows,d){for(let i=0;i<rows.length;i++)if(rows[i]?.link&&match(rows[i].link,d))return{position:i+1,url:rows[i].link};return{position:null,url:null}}
function prev(doc,d,k){const a=doc?.domains?.[d]?.keywords;if(!Array.isArray(a))return null;const x=a.find(v=>String(v.keyword||'').trim().toLowerCase()===k.trim().toLowerCase());return Number.isFinite(x?.position)?x.position:null}
function mainDoc(s){const x=read(F.r,null)||{domains:{}};x.domains=x.domains&&typeof x.domains==='object'?x.domains:{};x.last_updated=new Date().toISOString();x.country=s.country;x.country_code=s.country_code;x.language=s.language;x.search_engine=s.search_engine;x.max_position=Number(s.max_position);return x}
async function main(){console.log('==========================================\nIndia Google Rank Checker\n==========================================');const s={...D,...read(F.s,{})},cfg=read(F.k,null);if(!KEY)throw Error('ZENROWS_API_KEY environment variable is missing.');if(!Array.isArray(cfg?.domains)||!cfg.domains.length)throw Error('config/keywords.json must contain domains.');if(cfg.domains.length>Number(s.max_domains))throw Error('Maximum allowed domains: '+s.max_domains);const old=read(F.r,null),r=mainDoc(s),h=read(F.h,null)||{history:{}};h.history=h.history&&typeof h.history==='object'?h.history:{};const checked=r.last_updated;for(const dc of cfg.domains){const d=dom(dc.domain);if(!d)throw Error('Invalid domain: '+dc.domain);r.domains[d]={domain:d,keywords:[]};console.log('Domain: '+d);for(let i=0;i<dc.keywords.length;i++){const k=dc.keywords[i].trim();console.log('['+(i+1)+'/'+dc.keywords.length+'] '+k);const p=prev(old,d,k);try{const z=await serp(k,s),f=find(z.rows,d),status=f.position===null?'not_ranking':'ranking',rec={keyword:k,position:f.position,previous_position:p,change:Number.isFinite(p)&&Number.isFinite(f.position)?p-f.position:null,url:f.url,status,checked_at:checked};r.domains[d].keywords.push(rec);h.history[d]??={};h.history[d][k]??=[];h.history[d][k].push({checked_at:checked,position:f.position,status});console.log(f.position===null?'  NOT IN TOP '+s.max_position+' ('+z.pages+' pages checked)':'  Position: '+f.position+' ('+z.pages+' pages checked)')}catch(e){r.domains[d].keywords.push({keyword:k,position:null,previous_position:p,change:null,url:null,status:'error',error:e.message,checked_at:checked});h.history[d]??={};h.history[d][k]??=[];h.history[d][k].push({checked_at:checked,position:null,status:'error'});console.error('  ERROR: '+e.message)}if(Number(s.request_delay_ms)>0&&i<dc.keywords.length-1)await sleep(Number(s.request_delay_ms))}}h.last_updated=checked;write(F.r,r);write(F.h,h);console.log('==========================================\nRanking check completed\n==========================================')}
main().catch(e=>{console.error('FATAL ERROR: '+e.message);process.exit(1)});
